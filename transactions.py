class transactions:
    def __init__(self, name, type, date, amount, prior_balance, new_balance):
        self.name = name
        self.type = type
        self.date = date
        self.amount = amount
        self.prior_balance = prior_balance
        self.new_balance = new_balance


    print("Account History:")
    print(accountHistory[0].amount)
    self.balance += amount


    print("Balance Inquery:")
    print(balanceInquery[0].amount)
    self.balance += amount
